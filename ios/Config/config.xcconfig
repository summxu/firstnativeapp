PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER)$(BUNDLE_IDENTIFIER_APPEND)

BUNDLE_DISPLAY_NAME = FirstNativeApp
BUNDLE_IDENTIFIER = myapp.firstnativeapp

RUNTIME_URL = http:\/\/localhost:8080

PRODUCT_NAME = FirstNativeApp

HERMES_ENABLED = YES